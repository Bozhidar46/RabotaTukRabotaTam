@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @if (User.Identity.IsAuthenticated)
    {
        <a class="btn btn-success btn-lg" asp-controller="Category" asp-action="All">Categories</a>
    }
    <img style="vertical-align:middle" src="~/images/FindJob.jpg" alt="" width="70%" height="70%">
    @if (User.Identity.IsAuthenticated)
    {

        <h1 class="display-4">Lorem Ipsum!</h1>
        <div class="mb-3">
            <label for="" class="form-label">Lorem Ipsum</label>
            <input type="text" class="form-control">
        </div>
        <button type="submit" class="btn btn-primary">Search</button>
    }


</div>
